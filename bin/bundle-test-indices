#!/bin/bash

set -e

mkdir -p test/bundles

browserify --entry test/indices/active-constraint.js \
           --outfile test/bundles/active-constraint-loader.js

# Build the html to load.
./bin/build-test-html active-constraint

